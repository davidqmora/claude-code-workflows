---
description: Execute from requirement analysis to Blazor UI design document creation
argument-hint: Feature request, bug fix, or continuation directive
allowed-tools: ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash", "TodoWrite", "AskUserQuestion", "Skill", "Task"]
---

Load the `recipe-front-design` skill from this plugin using the Skill tool, then execute it for this request:

`$ARGUMENTS`

Follow that skill's design workflow, approval points, and document requirements exactly.
