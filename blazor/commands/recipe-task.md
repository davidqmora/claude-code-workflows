---
description: Execute focused Blazor UI tasks with rule-advisor metacognition
argument-hint: Task description, task file, or continuation directive
allowed-tools: ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash", "TodoWrite", "AskUserQuestion", "Skill", "Task"]
---

Load the `recipe-task` skill from this plugin using the Skill tool, then execute it for this request:

`$ARGUMENTS`

Follow that skill's focused execution, verification, and review steps exactly.
