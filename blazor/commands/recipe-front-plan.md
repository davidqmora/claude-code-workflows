---
description: Create a Blazor UI work plan from a design document and obtain approval
argument-hint: Design doc path or planning directive
allowed-tools: ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash", "TodoWrite", "AskUserQuestion", "Skill", "Task"]
---

Load the `recipe-front-plan` skill from this plugin using the Skill tool, then execute it for this request:

`$ARGUMENTS`

Follow that skill's planning workflow, review steps, and approval points exactly.
