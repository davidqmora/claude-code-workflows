---
description: Add integration or end-to-end tests to existing backend or fullstack code using Design Docs
argument-hint: Requirements, failing scenario, or continuation directive
allowed-tools: ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash", "TodoWrite", "AskUserQuestion", "Skill", "Task"]
---

Load the `recipe-add-integration-tests` skill from this plugin using the Skill tool, then execute it for this request:

`$ARGUMENTS`

Follow that skill's workflow, stop points, review requirements, and agent orchestration rules exactly.
