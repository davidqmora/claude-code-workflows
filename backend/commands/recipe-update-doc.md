---
description: Update existing design documents (Design Doc / PRD / ADR) with review
argument-hint: Document path and requested changes
allowed-tools: ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash", "TodoWrite", "AskUserQuestion", "Skill", "Task"]
---

Load the `recipe-update-doc` skill from this plugin using the Skill tool, then execute it for this request:

`$ARGUMENTS`

Follow that skill's document update workflow, review steps, and approval points exactly.
