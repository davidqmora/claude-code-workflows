---
description: Review frontend implementation for design compliance and security, with optional auto-fixes
argument-hint: Scope, file paths, or review directive
allowed-tools: ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash", "TodoWrite", "AskUserQuestion", "Skill", "Task"]
---

Load the `recipe-front-review` skill from this plugin using the Skill tool, then execute it for this request:

`$ARGUMENTS`

Follow that skill's review workflow, severity handling, and optional fix process exactly.
