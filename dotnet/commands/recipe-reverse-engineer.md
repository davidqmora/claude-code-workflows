---
description: Generate PRDs and Design Docs from an existing .NET or .NET+Blazor codebase
argument-hint: Codebase area, target scope, or continuation directive
allowed-tools: ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash", "TodoWrite", "AskUserQuestion", "Skill", "Task"]
---

Load the `recipe-reverse-engineer` skill from this plugin using the Skill tool, then execute it for this request:

`$ARGUMENTS`

Follow that skill's reverse-engineering workflow, artifact checkpoints, and review steps exactly.
