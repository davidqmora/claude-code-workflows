---
description: Investigate problem, verify findings, and derive solutions
argument-hint: Error, failing behavior, or investigation target
allowed-tools: ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash", "TodoWrite", "AskUserQuestion", "Skill", "Task"]
---

Load the `recipe-diagnose` skill from this plugin using the Skill tool, then execute it for this request:

`$ARGUMENTS`

Follow that skill's investigation, verification, and reporting workflow exactly.
