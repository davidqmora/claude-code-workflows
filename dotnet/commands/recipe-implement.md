---
description: Orchestrate the complete .NET implementation lifecycle from requirements through verified implementation
argument-hint: Requirements or continuation directive
allowed-tools: ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash", "TodoWrite", "AskUserQuestion", "Skill", "Task"]
---

Load the `recipe-implement` skill from this plugin using the Skill tool, then execute it for this request:

`$ARGUMENTS`

Follow that skill's orchestration rules, phase transitions, and approval points exactly.
